name: gpark
description: "Park. Scan. Done. — Parking validation via OCR for Google employees."
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: ^3.6.0
  flutter: ">=3.27.0"

dependencies:
  flutter:
    sdk: flutter
  flutter_riverpod: ^2.6.1
  google_sign_in: ^6.2.2
  camera: ^0.11.1
  google_mlkit_text_recognition: ^0.14.0
  http: ^1.3.0
  shared_preferences: ^2.5.0
  connectivity_plus: ^6.1.4
  uuid: ^4.5.1
  intl: ^0.20.2
  url_launcher: ^6.3.1

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0
  mocktail: ^1.0.4

flutter:
  uses-material-design: true
