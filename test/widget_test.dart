import 'package:flutter_test/flutter_test.dart';

void main() {
  testWidgets('App smoke test placeholder', (WidgetTester tester) async {
    // Full widget tests require mocking Google Sign-In and camera.
    // See test/utils/ and test/models/ for unit tests.
    expect(true, isTrue);
  });
}
